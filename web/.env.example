# URL nội bộ tới service API. Trình duyệt không dùng biến này — mọi lời gọi từ
# trình duyệt đi qua /api/* rồi Next chuyển tiếp (xem rewrites trong next.config.mjs).
API_INTERNAL_URL=http://127.0.0.1:8080

# Domain công khai, dùng cho metadata, sitemap và robots.
NEXT_PUBLIC_SITE_URL=https://aiedu365.example
